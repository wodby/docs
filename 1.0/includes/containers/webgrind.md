Webgrind displays Xdebug profiler traces and can generate call graphs. Enable
profiling in the PHP development service, reproduce the request, and then open
the Webgrind technical domain shown for the instance.

Profiler traces can contain application paths, function arguments, and timing
information. Enable the service only while diagnosing a trusted development or
staging environment, protect its domain, and remove traces when the
investigation is complete.
